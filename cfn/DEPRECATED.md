# DEPRECATED — CloudFormation Templates

These CloudFormation templates were part of the **original serverless infrastructure** (Lambda + API Gateway + DynamoDB + Step Functions).

The active infrastructure is defined in **`infra/`** (Terraform/CDK) and uses Docker + PostgreSQL locally.

These templates are kept for reference but are **not deployed or maintained**.
